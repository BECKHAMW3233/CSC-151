# Individual Contribution Statement

Canvas requires each group member to submit and clearly identify their own
individual work within the shared group project. The sections below are filled in
with a concrete record of what was actually done — fill in the blanks that are
personal to you, then adjust once your group has met and made its own decisions.

**Your name:** William Beckham
**Group members:** William Beckham, Christian Logan, Brandon Malave, Roberto Rendon-Valdez
**Course / section:** Fall 2026 Java Programming (CSC-151-0901)
**Instructor:** David Teter
**Date:** 2026-08-29 (updated 2026-09-01, 2026-09-02)

## This is groundwork for the group, not a finished group decision

**This is a Java Group Project.** What's in this repository so far is work I did
ahead of meeting the rest of the group, using Claude (Anthropic's AI assistant) as a
coding resource inside VS Code's Claude Code tool — which the assignment explicitly
allows: *"The Project is expected to be completed using AI as a resource, [given]
the time constraints of the project."*

I'm documenting it here as **my individual contribution to date**, not as the
group's finished or accepted direction. The group hasn't seen this yet and hasn't
decided anything. Once we meet, the group could:

- adopt this as our starting point and keep building on it,
- keep some parts (e.g. the architecture) but change others (e.g. the team, or the
  scope), or
- decide to go in a different direction entirely and not use this at all.

Any of those is a legitimate outcome — the point of doing this ahead of time was to
give the group something concrete to react to, not to lock in an approach before
anyone else had a say.

Being transparent about how the work itself happened: **Claude Code was used as a
tool to assist with research and with writing the code.** All of the logic — how
each task should be completed, and how to optimize the user experience — was
directed by me. A full record of the prompts given and what was done in response
is in [AI_SESSION_LOG.md](AI_SESSION_LOG.md).

## Where this fits in the course timeline

This is **Module 2 — Individual Project** work (10% of the course grade), per
William.

## A note on AI use and academic integrity

The course syllabus's Academic Integrity section lists "submitting the work
generated by an artificial intelligence (AI) tool... as if [it were] your own" as an
example of plagiarism. The syllabus's separate AI Guidelines section explains this
department is requiring AI use in programming courses, with specifics given
per-class — which is what this project's assignment prompt does explicitly
("The Project is expected to be completed using AI as a resource").

So the intent seems clear: AI use is required here, not prohibited, as long as it's
disclosed and the student's own direction/decisions are identified rather than
passed off as an unaided individual effort — which is the entire purpose of this
document. Given the stakes the syllabus lists for academic dishonesty, **confirm
with David Teter directly** that this document's level of AI-use disclosure matches
what he expects for this assignment, rather than assuming it's sufficient.

## Decisions and direction I made

These are the choices I made in putting this starting point together — the AI could
not have made any of these on its own, and each one shaped what got built:

1. **Chose a team to prototype with**: Carolina Panthers, based on proximity to
   Fayetteville, NC, since I had no strong team preference of my own and the group
   hadn't weighed in yet. Not a final decision — the app supports switching teams
   specifically so the group isn't locked into this choice.
2. **Set technical requirements up front**: Java only, GUI-based (not
   console/text), CSV-backed, and modular — matching the assignment's explicit
   asks, before any code existed.
3. **Rejected placeholder/sample data** once an initial version was working, and
   directed a full replacement with real, sourced 2026 roster/coaching/front-office
   data instead.
4. **Requested a zoom feature** for readability on a larger monitor, which led to
   the View menu and Ctrl+=/Ctrl+-/Ctrl+0 shortcuts.
5. **Asked directly "does this meet the requirements"** rather than assuming it
   did — which is what surfaced a real bug (changes weren't actually auto-saving,
   despite documentation claiming they were) before it could become a problem.
6. **Pushed on architecture correctness**: asked whether team names were
   hardcoded anywhere, which led to making the whole app data-driven with zero
   team-specific code — verified by an actual swap-and-revert test, not just an
   assertion.
7. **Scoped and directed a 32-team expansion**: decided to test whether the app
   could support every NFL team (not just one), specified the folder-per-team
   structure, and specified that data should be organized by division/region. This
   is exactly the kind of scope decision the group may want to revisit — it could
   just as easily be narrowed back to one team if that's what the group prefers.
8. **Set the data-quality bar** for that expansion: chose "full real rosters for
   every team" over lighter/placeholder alternatives, after being shown the
   tradeoff (a large research effort vs. a faster but thinner result).
9. **Kept the work moving through interruptions**: multiple background research
   agents hit usage limits mid-task; each time, I confirmed I wanted the work
   resumed rather than abandoned or restarted from scratch.
10. **Requested a real correctness audit** of the CSV data ("are the csv files
    proper and correct") instead of accepting "it works" at face value — this
    surfaced a real bug (a coach's age exceeded the edit dialog's allowed range)
    before it could crash the app for a real user.
11. **Requested this documentation** (comprehensive README, this contributions
    file) so the project's history and my role in it are traceable rather than
    assumed — and so the group has an honest record of what's actually done vs.
    still open, instead of a black box.
12. **Diagnosed a real cross-machine portability failure** — asked why the
    program ran fine on my original development PC but not on a laptop or other
    computers, rather than assuming the laptop was just misconfigured. This
    surfaced a genuine Java version mismatch (the laptop had no JDK at all, only
    an old Java 8 runtime, while the code needs Java 11+) instead of a code bug.
13. **Specified JDK 25** as the version to install, matching what I believe my
    course actually requires, rather than an arbitrary newer version.
14. **Reported and pinned down a real GUI display bug** — long text (a
    paragraph-length Note field, and columns like College/Title/Department)
    getting visually cut off — and specified exactly which screens were
    affected and how it should look (multiple wrapped lines, not a
    single clipped or scrollable line) once asked, rather than leaving it
    as a vague complaint for the AI to guess at.
15. **Asked for team graphics/branding per team**, then chose against using
    real official NFL logos once shown the trademark/licensing risk of
    embedding them in a public GitHub repository, opting instead for a
    generated color theme sourced from data already present in the CSVs.
16. **Directed the scope and intensity of that theme** — chose full,
    application-wide, bold/saturated coloring (not just a header banner, and
    not a subtle tint) after being shown the concrete Swing trade-off that
    decision required (switching the app's look-and-feel to Nimbus so buttons
    and other controls could be recolored consistently).
17. **Reported the theme's real readability problems directly from using the
    app** — flagged that menus and text weren't visible against some team
    colors, specifically the Team Info page's text, then flagged a second,
    separate readability failure in the menu bar once the first fix didn't
    resolve it. Specified the verification method myself, too: check the fix
    across multiple teams with dark color themes side by side, not just the
    one team already checked.
18. **Requested clickable column-header sorting** for the Players, Coaches,
    and Support Staff tables as a new feature, on top of the theming fixes.

## Next steps (as of 2026-08-29)

The plan is to hand this off and start actually collaborating, not to keep
building solo. Monday, I'll see a group member in person (285) and: hand off this
groundwork, find out whether he's already been in touch with the other two group
members, exchange contact information if we haven't already, and start
coordinating as a group from there — including revisiting the open questions
below together rather than me deciding them alone.

### Update (2026-09-01)

As of this date, **the rest of the group has not yet reviewed any of this work
and does not appear to be involved yet.** This remains solo prototype work done
ahead of that group involvement, not a group-reviewed or group-approved
decision — the same status as the original entry above, just noting that the
handoff/coordination described there hasn't resulted in group engagement so
far. Continuing to build and document individually in the meantime so there's
something concrete for the group to react to once they are involved.

## What exists right now (subject to the group's review)

A full history with dates and reasoning is in [CHANGELOG.md](CHANGELOG.md); the
short version:

- A modular Java/Swing app (`model` / `data` / `gui` packages) covering Players,
  Coaches, and Support Staff, with full Add/Edit/Remove, search, and filtering.
- CSV-backed persistence with a hand-written, quote-aware CSV parser.
- Support for all 32 NFL teams, each with its own folder of 4 CSV files, selectable
  from a startup picker grouped by Conference/Division, switchable at runtime.
- Real 2026 data for all 32 teams (rosters, coaching staffs, front offices),
  researched from official team sites and ESPN.
- A zoom feature, auto-save on every edit, and a completely data-driven design with
  no team names hardcoded anywhere in the source.
- A per-team color theme (`TeamTheme.java`), applied app-wide via a Nimbus
  look-and-feel, driven entirely by the `Primary Colors` field already in each
  team's `team_info.csv` — no logo images, after researching that no
  freely-licensed source of real NFL team logos exists for a public repo.
- Table display fixes: roster columns auto-size to their content instead of
  getting clipped, and the Team Info tab's paragraph-length fields (like Note)
  now wrap across multiple lines instead of showing one cut-off line.
- Fixed two real readability bugs in the per-team theme: Team Info Value-column
  text and the File/View/Help menu bar were both unreadable against certain
  team colors (verified across 4 teams spanning dark and light Primary Colors).
  Also added click-to-sort column headers on the Players/Coaches/Support Staff
  tables, with correct numeric sorting and a fixed Edit/Remove row-selection
  bug that sorting exposed.
- Along the way: found and fixed a real data-integrity bug (numeric bounds too
  tight for real NFL owners' ages/tenures), a real rendering bug (the team
  picker went blank under this environment's specific display/GPU setup), and a
  real cross-machine portability bug (the app requires a JDK 11+, which one
  laptop didn't have) — each diagnosed with actual evidence (screenshots,
  stderr checks, timing tests, reproducing the exact failure) rather than
  guessed at.

## Open questions for the group

These are genuinely unsettled — this is where the group's input actually changes
the project, not just rubber-stamps it:

- **Which team should we actually submit with?** Carolina Panthers is just what I
  happened to pick while putting this starting point together ahead of the group
  meeting. The group might want a team someone else cares about, or might want to
  keep the 32-team switcher as a feature instead of committing to one team.
- **Do we want the 32-team scope, or should we narrow back to one team?** Building
  out all 32 was my call to stress-test the architecture — the group may decide
  that's more than the assignment needs and prefer to simplify.
- **How do we want to divide the remaining work?** The `model` / `data` / `gui`
  split (see README) is one natural way to divide it among group members, but the
  group might prefer a different split, or prefer to divide by feature instead of
  by layer.
- **Does the group want to change direction entirely?** If someone has a
  substantially different idea for the project, this codebase doesn't have to be
  the starting point — it's here to make that conversation easier, not to
  foreclose it.

## What changed after group review

(Fill in once your group has actually met and decided something. Note what the
group kept, what it changed, what it rejected, and who ended up owning which part
of the codebase.)
